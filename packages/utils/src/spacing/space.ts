export let spaceX = <T extends unknown>(value: T) => ({
  "> * + *": {
    marginLeft: value,
  },
});

export let spaceY = <T extends unknown>(value: T) => ({
  "> * + *": {
    marginTop: value,
  },
});
